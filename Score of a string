class Solution {
    public int scoreOfString(String s) {
        int res = 0;
       for(int i=1;i<s.length();i++) {
        char ch = s.charAt(i);
        if(ch>='a' && ch<='z'){
            res += Math.abs(s.charAt(i)-s.charAt(i-1));
        }
       } 
       return res;
    }
}
